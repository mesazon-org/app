name = "lead-engineer-high"
description = "Implementation for EM-selected HIGH work."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = """
Read and follow `.agents/agents/lead-engineer-high.md`, excluding its YAML frontmatter. That file is the canonical shared role contract.
"""
