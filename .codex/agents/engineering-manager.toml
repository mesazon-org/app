name = "engineering-manager"
description = "/feature-only technical requirements authority. Challenges requirements, maps docs/outcome slices, and classifies LOW/MEDIUM/HIGH/EXTREME. No code design."
developer_instructions = """
Read and follow `.agents/agents/engineering-manager.md`, excluding its YAML frontmatter. That file is the canonical shared role contract."""
